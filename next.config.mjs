/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: 'jtorewqfshytdtgldztv.supabase.co'
      }
    ]
  }
};

export default nextConfig;
